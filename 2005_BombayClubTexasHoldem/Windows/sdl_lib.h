/////////////////////////////////////////////
// sdl_lib.h

/////////////////////////////////////////////
#ifndef _SDL_LIB_H_
#define _SDL_LIB_H_
/////////////////////////////////////////////

/////////////////////////////////////////////
#include "sdl.h"
#include "sdl_image.h"
#include "typedefs.h"
/////////////////////////////////////////////

/////////////////////////////////////////////
class SDL_lib
{
public:
	SDL_lib( );
	~SDL_lib( );

	SDL_Surface* load_image( std::string filename );
	void apply_surface( position_t x, position_t y, 
				    SDL_Surface* source, SDL_Surface* destination );
	void init( );
	void load_files( );
	void clean_up( );

	SDL_Surface *queenClubs;
	SDL_Surface *background;
	SDL_Surface *screen;

	SDL_Event my_event;
};
/////////////////////////////////////////////

/////////////////////////////////////////////
#endif
/////////////////////////////////////////////